// 1. How to declare a variable using let and const
const matharName = 'Ambiya';
let season = 'rainy';
season = 'winter';

// 3. Conditions

// 6. basic conditons: >,< ===, !==, <=, >=
// Multiple conditions: &&, ||

if (matharName === 'Ambiya' || season === 'rainy') {
    
}
else if (matharName === 'Ambiya') {
    
}
else {
    
}

// 3. Array diclare
// index
// length, push, pop

const number = [48, 45, 85]
number[0] = 56;

// 4. for loop

for (let i = 0; i < number.length; i++){
    const number = number[i];
    console.log(number);
}

// 5. Function

function maltiPle(num1, num2) {
    const result = num1 * num2;
    return result;

}

// 6. Object
